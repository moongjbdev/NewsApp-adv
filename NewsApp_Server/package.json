{
  "name": "newsapp_server",
  "version": "2.0.0",
  "description": "Backend API for NewsApp v2.0 - News reading app with NewsData.io integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset-indexes": "node scripts/reset-indexes.js",
    "setup": "node scripts/setup.js",
    "lint": "echo \"No linter configured\"",
    "format": "echo \"No formatter configured\"",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": ["news", "api", "express", "mongodb", "nodejs", "newsdata", "caching"],
  "author": "moongjbdev",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.5.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
